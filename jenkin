pipeline {
    agent any

    stages {
     
        stage('build') {
            steps {
               echo 'Build Success'
            }
        }
        stage('test') {
            steps {
              sh 'echo "Test success"'
            }
        }
        stage('deploy') {
            steps {
               sh 'echo "Deploy success"'
            }
        }
    }
}
